import React, {Component} from 'react';
import './App.css';
import LoginRegister from './Login&Registration/LoginRegister';
import Registration from './Login&Registration/Registration';

export default class App extends Component {
  
  render = () => {
      return (
        <LoginRegister/>   
      );
  }
}
